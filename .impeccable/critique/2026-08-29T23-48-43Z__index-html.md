---
target: index.html (home page)
total_score: 17
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 2
timestamp: 2026-08-29T23-48-43Z
slug: index-html
---
Method: dual-agent (A: a277805a93ad9aa7d · B: ae589867113cb01e6)

NOTE: the detector was running DEGRADED (htmlparser2, css-select, css-tree, domutils missing) and reported 1 finding. After installing those four packages into the skill's node_modules, the same scan returned 18. All counts below are full-mode.

# Design Health Score — index.html

| # | Heuristic | Score | Key issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Live Open/Closed badge sits at 83% page depth, never says until when; hamburger never becomes an X |
| 2 | Match System / Real World | 2 | "Open until 9:30pm most days" is false; "Find a Location" for one shop; title sells ice cream, shop sells loaded desserts |
| 3 | User Control and Freedom | 1 | Auto-scrolling carousel, no pause, tabIndex:-1; Escape closes neither cart nor nav; no skip link |
| 4 | Consistency and Standards | 2 | Five typefaces loaded, four in use, no rule; --cream and --cream-alt identical; nav "Contact" vs footer "Locations", same anchor |
| 5 | Error Prevention | 2 | Cart assembles orders it cannot price and displays "$0.00 Total" |
| 6 | Recognition Rather Than Recall | 2 | #hours has no entry point anywhere; two contradictory hours statements 1,150px apart |
| 7 | Flexibility and Efficiency | 1 | Scored, not n/a — half the audience IS the efficiency audience, and no fast path exists |
| 8 | Aesthetic and Minimalist Design | 2 | Strong hero and reviews band vs four content-free chips, four emoji cards, a #gallery with no gallery |
| 9 | Error Recovery | 2 | Empty cart: "Your cart is empty. or call us instead" — orphaned conjunction, no route back to menu |
| 10 | Help and Documentation | 1 | WhatsApp handoff unexplained; 17 of 18 products have no price anywhere on the site |
| Total | | 17/40 | Poor — major UX work needed |

All ten heuristics scored (none n/a). H7/H10 are commonly n/a on Persuade surfaces; not here, because PRODUCT.md establishes half the traffic is locals doing a 60-second lookup.

# Design Specificity Verdict

Partially specific. ~60% of the page's vertical extent is category-interchangeable furniture, bookended by three genuinely authored moments.

Authored: the hero photograph and its measured 0.3 scrim; the plum reviews band with one continuous gradient and a single reserved pink CTA; the hours table that tints Monday and stacks Friday.

Interchangeable: the four hero chips (leaf/droplet/heart/sparkle, no product named); the Why Us emoji grid; "Ready for your happy place?"; "Follow the Sweetness"; and the eyebrow -> centred H2 -> centred lede rhythm repeated identically SEVEN times at identical scale.

Sharpest finding: the menu includes loaded cups, frosty nachos, loaded cookies, acai bowls and custom Flurrs. "Flurr" is the business's own coinage — its only proprietary noun — and it appears ZERO times on the home page. Neither do nachos, loaded cookies, or acai (except in the self-written quote card).

DETERMINISTIC SCAN — 18 findings in index.html, 21 in menu.html:
- undersized-ui-text x5 ("Order Now" @10px; four hero chips @10.56px)
- gpt-thin-border-wide-shadow x3 (1px border + 40/40/60px blur)
- overused-font x2 (Plus Jakarta Sans = 50% of all text; Fraunces)
- low-contrast x1 (3.9:1 hover, white on #f32379)
- side-tab, cramped-padding, clipped-overflow-container, cream-palette, skipped-heading x1 each (skipped-heading: h2 "Opening Hours" -> h4 "Explore")

menu.html is worse on one axis: 16 undersized-ui-text findings.

VISUAL OVERLAYS — injection succeeded; in-page detector reported 135 anti-patterns. ~120 collapse to one root cause: span.fr-g-badge, the 15x15px Google "G" replica per review card, white 9.6px glyph on #fbbc05 = 1.70:1. Treat as ~15 distinct issues; it is a brand-mark reproduction where recolouring is arguably the wrong fix. The overlay caught one thing the CLI structurally cannot: kicker-above-heading x6 — every section eyebrow on the page. Overlay server started on port 8400 and verified stopped.

# Overall Impression

The craft is unevenly distributed: the parts that were thought about are excellent, the parts never questioned are template defaults that survived every pass.

js/hours.js is the best thing here — Intl.DateTimeFormat with timeZone 'Pacific/Auckland', Friday modelled as two ranges, re-evaluated every 60s. Correct, honest, and invisible: it renders one badge at 83% page depth in a section no navigation link points to.

That is the page in miniature. 49 real reviews including an unhidden 1-star, 31 real photographs, correct hours logic, a differentiated halal fact — and the home page shows 3 photographs, buries the reviews at 3,642px, contradicts its own hours data, and never mentions halal.

Biggest opportunity: stop asserting and start showing. Delete the hero chips, the emoji grid and the self-quote; put the desserts, reviews and live hours in their place. The page gets shorter AND more persuasive.

# What's Working

1. The reviews band's honesty. 49 verbatim reviews, real names and dates, including one negative review that could have been deleted and was not. The plum treatment gives it deserved weight.
2. js/hours.js. Correct timezone, correct Friday split, live re-evaluation. PRODUCT.md's "awkward truths stay visible" executed exactly right. It works; it is in the wrong place.
3. Reduced motion is properly honoured — css/styles.css:1460 kills all animations and 27 transitions, js/reviews.js:362 gates the marquee, js/script.js swaps smooth scroll for auto. No animation escapes the guard. :focus-visible exists (css/styles.css:63); zero `outline: none` rules anywhere.

# Priority Issues

## [P0] The hours are wrong, and the correct ones have no entry point

index.html:278 says "Open until 9:30pm most days · Saturdays from 11:30am". Actual: Sun 7:30, Tue 7:30, Wed 7:30, Thu 9:30, Fri 9:30, Sat 9:30 — exactly half the open days close at 7:30pm. Meanwhile #hours is linked from nowhere: zero `href="#hours"` in either file. Nav says "Contact", footer says "Locations". Mobile page is 8,787px; #hours starts at 7,298px (83% down).

WHY: a local reads that line, walks over at 8:15pm Wednesday, finds the shop shut. The site actively misinforms the audience it exists to serve, contradicting correct data 1,150px below.

FIX: delete the hand-written summary; render a today-line from hours.js. Add Hours to nav and footer. Put the live badge in the header. Separately: .hero-ctas is styled at css/styles.css:553 with NO matching markup — the hero CTAs were designed and never shipped, so 845px of prime real estate makes no offer and answers no question.

COMMAND: /impeccable clarify, then /impeccable layout

## [P0] The cart is broken, worse than PRODUCT.md recorded

menu.html contains ONE .add-to-cart-btn and THREE data-price attributes for 18 products. 17 of 18 products cannot be added at all — the $0.00 defect was an understatement. The cart icon sits in the home page header on every viewport; opening it yields "Your cart is empty. / or call us instead" — dangling "or" (the button it refers to is hidden) and no link to the menu.

WHY: a first-timer taps a cart icon expecting online ordering, gets a dead end, concludes the site is broken — at the moment the page was meant to convert them.

FIX: given ordering is a stand-in, either remove the cart from the home header (keep on /menu), or convert to a "Pickup list" carrying item names into the WhatsApp draft with no prices and no Total. Never render "$0.00". Add "Browse the menu ->" to the empty state; drop the orphaned "or".

COMMAND: /impeccable harden

## [P1] Three dessert photographs on a page selling desserts

The home page renders 4 img elements: the logo twice and two promo photos, plus one CSS background hero. images/ holds 31 real photographs, 13 of them (~16.5 MB) referenced by nothing at all. The section with id="gallery" contains two text-and-image blocks and no gallery.

WHY: PRODUCT.md says the desserts and reviews are the argument; reviewers name how the desserts look as the uncopyable thing. First-timers arrive from Instagram and land on a page with less imagery than the grid they left.

FIX: rebuild #gallery as a real photo grid of 8-12 existing images, captioned with actual item names, linking to /menu. Convert source PNGs to WebP; add explicit width/height (none of the 4 images has dimension attributes, so all shift layout on load).

COMMAND: /impeccable bolder

## [P1] The mobile primary CTA fails contrast and legibility simultaneously

"Order Now" at 390px has background-color rgba(0,0,0,0), rendering as unstyled pink text on cream at 10px = 3.68:1. Fails the 4.5:1 contrast floor and the 11px legibility floor at once, as the primary CTA. Desktop is fine (14.4px, 168x43); mobile-only.

Cluster: 18 of 28 interactive elements at 390px are under 44x44 — hamburger 36x31, cart 34x34, nav links 342x23, footer links ~70x18. All three header controls are sub-44px and touching.

Also verified: closed mobile nav stays in tab order (opacity:0; pointer-events:none) = five invisible focus stops, WCAG 2.4.7 fail. Reviews carousel is keyboard-unreachable and unstoppable (tabIndex:-1, no arrows, no pause) = WCAG 2.2.2 Level A and 2.1.1 fail. At 320px the hamburger's right edge computes to 330px, clipped off-screen = WCAG 1.4.10 fail, unnavigable at 400% zoom.

#f32379 is a recurring failure: passes on black (5.33:1), fails on cream (3.68:1) for active nav link, section eyebrows, Order Now. --pink-deep #BB0E63 is already in the token set at 5.84:1.

COMMAND: /impeccable audit

## [P2] Zero discovery metadata on a page whose traffic is Google and Instagram

No meta description, no Open Graph, no Twitter card, no favicon, no LocalBusiness JSON-LD — zero matches in both files. Title reads "Frosty Haven — Handcrafted Ice Cream" for a shop whose signature items are loaded cups, frosty nachos and acai bowls.

WHY: PRODUCT.md says discovery runs through Google and Instagram. Every shared URL renders as a bare link with no image or description, for a business whose entire argument is visual. Google has no machine-readable hours for the knowledge panel.

FIX: add description, OG title/description/image (promo-milkshake-trio.jpg), a favicon, and LocalBusiness JSON-LD with openingHoursSpecification. Retitle to name what the shop actually sells.

COMMAND: /impeccable harden

# Persona Red Flags

JORDAN (first-timer): scrolls 8,787px and never learns what anything costs; only price signals are inside review text. Taps ORDER NOW expecting a menu, gets a tel: dial for a shop whose prices they have never seen — and on desktop the link does nothing. "Leave Us a Google Review" is the most prominent button in the reviews band, addressed to someone who has never visited. index.html:176 puts "A dessert destination like no other..." attributed "— Frosty Haven" in the same three-card row as the real 4.8 Google and 5.0 Facebook ratings; it reads as a third review, and once noticed it discounts the two genuine ratings beside it. This is the one place the page violates PRODUCT.md principle 3.

CASEY (one-handed, interruptible, slow connection): body.is-loading { opacity: 0 } is cleared only by js/page-transition.js, the fourth of four render-blocking scripts with no defer — blank cream rectangle on a slow connection, permanently invisible with JS blocked. Phone number at 6,147px of 8,787. Escape does not close the cart (no keydown listener in js/script.js). At 390px the subhead lands on the white FROSTY print on the cup — white type over white artwork.

SAM (screen reader, keyboard-only, 200% zoom): the first heading in the DOM is h3 "Your Order" (cart panel) before the h1 — heading-jump navigation opens on a shopping cart. 39 collapsed reviews are announced while the button reports aria-expanded="false", and aria-controls names a different element than the one that opens. No skip link. Hero is a CSS background with no text alternative; .fr-stars are aria-hidden with no equivalent, so the 5-star cards cannot be distinguished from the 1-star one.

MEREANA (returning local, derived from PRODUCT.md): 8:40pm Wednesday on Featherston Street. Nothing above the fold says whether the shop is open. She reads "Open until 9:30pm most days" and walks over. Wednesday closes at 7:30pm. The site sent her to a closed shop.

# Minor Observations

- --cream and --cream-alt are the same value; the alt token is a silent no-op still referenced.
- Five font families / 15 weight variants requested; 9 faces painted. Six downloaded and never used: PJS 700 & 800, Cormorant 500, Fraunces 600, Manrope 400 & 600.
- logo.png is 1717x916 / 414 KB rendered at 146x78 (11.7x oversized), eager, loaded twice. Page image payload ~1.38 MB.
- Brand logo links to #top, which is main — scrolls to just under the header, not the true top.
- Footer "Gallery" links to #gallery, whose visible heading is "The Frosty Haven Experience".
- Hamburger has aria-expanded wired correctly but no visual open state; bars never become an X.
- Footer address wraps as "Palmerston / North Central 4410", splitting the suburb mid-phrase.
- Halal — a genuine differentiating fact PRODUCT.md flags as available — appears nowhere on the page.
- Console and network are clean: zero errors, zero 404s, at every viewport.

FALSE POSITIVES DISCARDED: the 8px horizontal overflow at 768/1280 (real scrollWidth delta, but overflow-x: clip makes it non-scrollable; true effect is 7.5px of .value-promo clipped); the 20+ #frTrack cards past the viewport (intentional scrolling track); cramped-padding on the hero (full-bleed by design); ~120 of 135 overlay findings collapsing to one badge. Assessment B also caught its own error — an initial 1.07:1 reading on #cartCheckout came from a background walker that missed the gradient; the real figure is 3.90:1.

# Questions to Consider

1. If you deleted every element that asserts rather than shows — the four hero chips, the Why Us grid, the self-authored quote — what remains, and is it more persuasive or less?
2. The reviews are the strongest asset and they begin at 3,642px. What happens if one real, named review is the first thing under the H1?
3. 31 photographs on disk, 3 on the home page. What is the argument for the other 28?
4. The JavaScript knows correctly and to the minute whether the shop is open, including the Friday split, in the right timezone. Why does that appear once, as a badge, 83% of the way down?
5. The cart can hold 1 product out of 18. Is a broken checkout better or worse than an honest line saying "Call or message and we'll price it for you"?
6. Reviewers name two uncopyable things: how the desserts look, and the owner and staff. The page shows the first three times and the second zero times. interior-collage.jpg and counter-closeup.jpg exist. What changes if the shop itself is on the page?
7. The word "Flurr" — the only noun this business owns — appears zero times on its home page. What is the page selling instead?
